#include <bits/stdc++.h>
using namespace std;
int main(){
  char a = 'a';
  // a 는 숫자 97이다.
  cout << (int)a << '\n'; //a를 정수형으로 변환
  cout << (int)a - 97 << '\n'; 
  //정수형으로 변환한 a
  // 97 - 97 진행
  // 결과 0 
  cout << (int)a - 'a' << '\n';
  //
}