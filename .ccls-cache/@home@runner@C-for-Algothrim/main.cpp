#include <bits/stdc++.h>
using namespace std;
int main(){
string s = "1";
string s2 = "amumu";
cout << atoi(s.c_str()) << '\n';
cout << atoi(s2.c_str()) << '\n';
  //atoi(변수명.c_str)
  //atoi(s.ctr())
  //입력받은 문자열이 문자면 0를 반환
  //입력받은 문자열이 문자가 아닐 경우, 숫자를 반환
return 0;
}
//출력 1
//0
